# No API key needed — the app authenticates through your local Claude Code login
# (the `claude` CLI you use in the terminal), via the Claude Agent SDK.
#
# Optional: pin a specific model. Leave unset to use your Claude Code default.
# AGENT_MODEL=claude-opus-4-8
